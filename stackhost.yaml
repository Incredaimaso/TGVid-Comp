user:
  name: cevilion 
  github: incredaimaso

language: "python"

commands:
  build:
    - "pip3 install -r requirements.txt"
    - "pip install lxml_html_clean"
    - "pip list | grep lxml"
  package:
    - "nano inxi"
  start:
    - "python3 -m bot"

env:
  - "ENVIRONMENT=production"
