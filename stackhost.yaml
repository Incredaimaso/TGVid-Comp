user:
  name: "ishikki"
  github: "ishikki-akabane"

runtime:
  image: "python-nodejs"

commands:
  package: ""
  build:
    - "pip install --no-cache -r requirements.txt"
  start: "python bot.py"

repository:
  branch: "master"
  auto_deploy: true
