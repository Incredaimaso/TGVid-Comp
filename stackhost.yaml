user:
  name: cevilion 
  github: incredaimaso

language: "python"

commands:
  build:
    - docker:
     worker: Dockerfile
  package:
    - "nano inxi"
  start:
    - "python3 -m bot"

env:
  - "ENVIRONMENT=production"
